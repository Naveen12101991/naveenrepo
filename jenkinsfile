pipeline {
    agent any

    stages {
        stage('downloadgit') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/Naveen12101991/naveenrepo.git']]])
            }
        }
    }
}
